#!/usr/bin/env bash
# Rebuild this project's promo videos (real screenshots → ffmpeg motion).
# Reusable generator lives at repo root.
cd "/home/flegartech/Downloads/all in one" && node build-videos.mjs YoutubeFocus-master
